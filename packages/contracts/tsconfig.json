{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../config/tsconfig/node.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "types": ["node", "mocha", "chai"]
  },
  "include": ["hardhat.config.ts", "scripts/**/*.ts", "test/**/*.ts"],
  "exclude": ["artifacts", "cache", "dist", "node_modules", "typechain-types"]
}
